.container
  .row
    .col-md-4.col-md-offset-4
      form(name="form", novalidate)
        h2.form-signin-heading Please log in
        .form-group(
          ng-class="{'has-error': form.username.$invalid && form.username.$touched}"
        )
          label.sr-only(for="username") Username
          input#username.form-control(
            name="username", ng-model="username", placeholder="Username",
            type="text", required
          )

          div(
            ng-messages="form.username.$error",
            ng-if="form.username.$touched"
          )
            label.control-label(
              ng-message="required"
            ) A username is required.

        .form-group(
          ng-class="{'has-error': form.password.$invalid && form.password.$touched}"
        )
          label.sr-only(for="password") Password
          input#password.form-control(
            name="password", ng-model="password", placeholder="Password",
            type="text", required
          )

          div(
            ng-messages="form.password.$error",
            ng-if="form.password.$touched"
          )
            label.control-label(
              ng-message="required"
            ) A password is required.

        button.btn.btn-lg.btn-primary.btn-block(
          ng-click="authenticate(username, password);", ng-class="{disabled: form.$invalid}"
        ) Log in
